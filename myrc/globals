############################################################
# globals
############################################################

export TERMINAL=alacritty
export EDITOR=nvim
export BROWSER=firefox
export PAGER=less
export NET_DEVICE=wlp2s0

# XDG HOME
export XDG_DATA_HOME=~/.local/share
export XDG_CONFIG_HOME=~/.config
export XDG_STATE_HOME=~/.local/state
export XDG_BIN_HOME=~/.local/bin
export XDG_CACHE_HOME=~/.cache

# XDG DIR
export XDG_DESKTOP_DIR=~/Desktop
export XDG_DOCUMENTS_DIR=~/Documents
export XDG_DOWNLOAD_DIR=~/Downloads
export XDG_MUSIC_DIR=~/Music
export XDG_PICTURES_DIR=~/Pictures
export XDG_PUBLICSHARE_DIR=~/Share
export XDG_TEMPLATES_DIR=~/Templates
export XDG_VIDEOS_DIR=~/Videos

# others
export FZF_DEFAULT_COMMAND="fd --type f --hidden --follow --exclude .git"
export FZF_DEFAULT_OPTS="--layout=reverse --inline-info"
export LESSHISTFILE=${XDG_CACHE_HOME}/less/history
export DOTDIR=${HOME}/.config
export XINITRC=${XDG_CONFIG_HOME}/rc/.xinitrc
export ZDOTDIR=${XDG_CONFIG_HOME}/zsh
export GTK2_RC_FILES=${XDG_CONFIG_HOME}/gtk-2.0/gtkrc
export HISTFILE=${XDG_STATE_HOME}/bash/history
export PASSWORD_STORE_DIR=${XDG_DATA_HOME}/.password-store
export GNUPGHOME=${XDG_DATA_HOME}/.gnupg
export XDG_NSS_USER_PATH3=${XDG_DATA_HOME}/pki
export XAUTHORITY=${XDG_DATA_HOME}/.Xauthority
export MBSYNCRC=${XDG_CONFIG_HOME}/myrc/mbsyncrc
export NOTMUCH_CONFIG=${XDG_CONFIG_HOME}/myrc/notmuch-config
export STARSHIP_CONFIG=${XDG_CONFIG_HOME}/myrc/starship.toml

# setting locale for dmenu
export LC_CTYPE=en_US.UTF-8
export LANG=en_US.UTF-8

# rust
export CARGO_INCREMENTAL=1
export RUST_BACKTRACE=1
export RUSTUP_HOME=${XDG_DATA_HOME}/rustup
export CARGO_HOME=${XDG_DATA_HOME}/cargo

# view man pages in bat
export MANPAGER="sh -c 'col -bx | bat -l man -p'"
export MANROFFOPT="-c"

# PATH
export PATH=${PATH}:${XDG_BIN_HOME}
export PATH=${PATH}:${XDG_CONFIG_HOME}/vifm/scripts
export PATH=${PATH}:${XDG_DATA_HOME}/cargo/bin
export RUST_SRC_PATH="$(rustc --print sysroot)/lib/rustlib/src/rust/library"
export XCURSOR_PATH=${XCURSOR_PATH}:${XDG_DATA_HOME}/icons
